import React from 'react'
import ReactDOM from 'react-dom/client'
import TrustChainAI from './trustchain'
import './styles.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TrustChainAI />
  </React.StrictMode>,
) 